{
  "name": "supertab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "postbuild": "rm -f dist/style.css",
    "build:firefox": "vite build && cp public/manifest-firefox.json dist/manifest.json",
    "dev": "vite build --watch --mode development"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  }
}
